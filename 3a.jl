println("enter the principle amount:")
p = parse("float64", readline() )
println("enter the rate of interest:")
r = parse("float64", readline() )

curent= p
for year in 1:10
    current <=currenrt * r / 100

     println("year $year :\$ (round(p digits=2))")

    if current <=current*2
        println("the anount is exceded , stoping the calculation.")
        break   
    end
end
